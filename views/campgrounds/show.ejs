<% layout('layouts/boilerplate') %>
    <h1><%= campgrounds.title %></h1>
    <h2><%= campgrounds.location %></h2>
    <img src="<%= campgrounds.image %>" alt="">
    <p><%= campgrounds.description %></p>
    <form action="/campgrounds/<%= campgrounds._id %>/?_method=DELETE" method="POST">
        <button>Delete</button>
    </form>
    <footer>
        <a href="/campgrounds">All Campgrounds</a>
        <a href="/campgrounds/<%= campgrounds._id %>/edit">Edit Campground</a>
    </footer>
